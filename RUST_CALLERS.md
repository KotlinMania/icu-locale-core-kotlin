# Rust callers — `icu-locale-core-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `icu_locale_core` | `icu_locale_core` | 11 | 3 | 3 | 1 |

## `icu_locale_core`

### Inbound symbols (7 distinct, 11 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `icu_locale_core`.

#### `icu_locale_core::Locale` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-protocol` `src/num_format.rs:6` — `use icu_locale_core::Locale`

#### `icu_locale_core::locale` — 4 site(s) (0 codex, 4 other, 2 caller crates)

- `icu-decimal-kotlin/icu_decimal` `benches/fixed_decimal_format.rs:13` — `use icu_locale_core::locale`
- `icu-decimal-kotlin/icu_decimal` `src/format.rs:108` — `use icu_locale_core::locale`
- `icu-decimal-kotlin/icu_decimal` `src/lib.rs:106` — `use icu_locale_core::locale`
- `icu-provider-kotlin/icu_provider` `src/hello_world.rs:360` — `use icu_locale_core::locale`

#### `icu_locale_core::preferences::define_preferences` — 2 site(s) (0 codex, 2 other, 2 caller crates)

- `icu-decimal-kotlin/icu_decimal` `src/lib.rs:107` — `use icu_locale_core::preferences::define_preferences`
- `icu-provider-kotlin/icu_provider` `src/hello_world.rs:16` — `use icu_locale_core::preferences::define_preferences`

#### `icu_locale_core` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `icu-provider-kotlin/icu_provider` `src/lib.rs:182` — `pub_use icu_locale_core`

#### `icu_locale_core::DataLocale` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `icu-provider-kotlin/icu_provider` `src/request.rs:22` — `pub_use icu_locale_core::DataLocale`

#### `icu_locale_core::preferences::LocalePreferences` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `icu-provider-kotlin/icu_provider` `src/marker.rs:9` — `use icu_locale_core::preferences::LocalePreferences`

#### `icu_locale_core::preferences::extensions::unicode::keywords::NumberingSystem` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `icu-decimal-kotlin/icu_decimal` `src/lib.rs:132` — `pub_use icu_locale_core::preferences::extensions::unicode::keywords::NumberingSystem`
