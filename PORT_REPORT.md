# Port report — `icu-locale-core-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `icu_locale_core`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 11 `use` sites from 3 other repos
- **Outbound**: 6 `use` sites to 1 other repos
- **Unresolved external**: 5 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `icu_locale_core` | `icu_locale_core` | 11 | 1 | COD-63 | 63 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (11 edges, 3 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `icu-decimal-kotlin` | `icu_decimal` | 5 |  |
| `icu-provider-kotlin` | `icu_provider` | 5 |  |
| `codex-kotlin` | `codex-protocol` | 1 | ✅ |

### Symbol-level demand (7 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `icu_locale_core::Locale` | 1 | 1 | 0 | 1 |
| `icu_locale_core::locale` | 4 | 0 | 4 | 2 |
| `icu_locale_core::preferences::define_preferences` | 2 | 0 | 2 | 2 |
| `icu_locale_core` | 1 | 0 | 1 | 1 |
| `icu_locale_core::DataLocale` | 1 | 0 | 1 | 1 |
| `icu_locale_core::preferences::LocalePreferences` | 1 | 0 | 1 | 1 |
| `icu_locale_core::preferences::extensions::unicode::keywords::NumberingSystem` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 7 symbols)

##### `icu_locale_core::Locale` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-protocol` `src/num_format.rs:6` — `use icu_locale_core::Locale`

##### `icu_locale_core::locale` (4 sites, 0 codex, 4 other, 2 caller crates)

Other workspace call sites:

- `icu-decimal-kotlin/icu_decimal` `benches/fixed_decimal_format.rs:13` — `use icu_locale_core::locale`
- `icu-decimal-kotlin/icu_decimal` `src/format.rs:108` — `use icu_locale_core::locale`
- `icu-decimal-kotlin/icu_decimal` `src/lib.rs:106` — `use icu_locale_core::locale`
- `icu-provider-kotlin/icu_provider` `src/hello_world.rs:360` — `use icu_locale_core::locale`

##### `icu_locale_core::preferences::define_preferences` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `icu-decimal-kotlin/icu_decimal` `src/lib.rs:107` — `use icu_locale_core::preferences::define_preferences`
- `icu-provider-kotlin/icu_provider` `src/hello_world.rs:16` — `use icu_locale_core::preferences::define_preferences`

##### `icu_locale_core` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `icu-provider-kotlin/icu_provider` `src/lib.rs:182` — `pub_use icu_locale_core`

##### `icu_locale_core::DataLocale` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `icu-provider-kotlin/icu_provider` `src/request.rs:22` — `pub_use icu_locale_core::DataLocale`

##### `icu_locale_core::preferences::LocalePreferences` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `icu-provider-kotlin/icu_provider` `src/marker.rs:9` — `use icu_locale_core::preferences::LocalePreferences`

##### `icu_locale_core::preferences::extensions::unicode::keywords::NumberingSystem` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `icu-decimal-kotlin/icu_decimal` `src/lib.rs:132` — `pub_use icu_locale_core::preferences::extensions::unicode::keywords::NumberingSystem`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `serde-kotlin` | 6 | 4 | `serde` | COD-128 | 1 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `writeable` | 19 | 5 | `icu_locale_core` |
| `databake` | 5 | 5 | `icu_locale_core` |
| `litemap` | 4 | 2 | `icu_locale_core` |
| `displaydoc` | 3 | 1 | `icu_locale_core` |
| `tinystr` | 2 | 2 | `icu_locale_core` |
